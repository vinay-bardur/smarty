{
  "name": "smarty-backend",
  "version": "1.0.0",
  "description": "Backend for Smarty Smart Timetable with Supabase Edge Functions and Groq AI",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "lint": "eslint src/**/*.ts",
    "deploy": "supabase functions deploy",
    "deploy:detect": "supabase functions deploy detect-conflicts",
    "deploy:suggest": "supabase functions deploy generate-suggestions",
    "deploy:export": "supabase functions deploy export-timetable"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
